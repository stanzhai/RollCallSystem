CREATE TABLE [dbo].[RecordIndex] (
    [ID]       UNIQUEIDENTIFIER NOT NULL,
    [Date]     DATETIME         NOT NULL,
    [CourseID] UNIQUEIDENTIFIER NOT NULL,
    [ClassID]  INT              NOT NULL
);

